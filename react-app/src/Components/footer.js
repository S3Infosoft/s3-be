import React from "react";
import { makeStyles, Typography, Card } from "@material-ui/core";

const footer = () => {
  return (
    <div>
      <span>footer</span>
    </div>
  );
};

export default footer;
